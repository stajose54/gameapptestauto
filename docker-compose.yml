version: "3.8"
services:
  gameapptestauto:
    image: stajose/snakegamesonar:latest
    build: .
    container_name: snakegamesonar
    ports: 
      - "80:3000"
    environment: 
      - PORT=3000
    volumes:
      - ./:/app
    command: ["node", "app.js"]
